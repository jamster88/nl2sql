.git
.venv
output
*.db
__pycache__
*.pyc
.DS_Store

# The GUIs' build inputs and outputs. node_modules alone is over four
# thousand files and a hundred megabytes *each*, and both images install
# their own from the lockfile -- so sending them is a hundred megabytes
# uploaded to the daemon to be thrown away.
gui/node_modules
gui/dist
gui/coverage
review/gui/node_modules
review/gui/dist
review/gui/coverage
